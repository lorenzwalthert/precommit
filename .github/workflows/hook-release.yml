on:
  pull_request:
    types:
      - closed

jobs:
  if_merged:
    if: github.event.pull_request.merged == true && gihub.head_ref == 'hook-dependencies-update'
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo The PR was merged
